---
const items = [
  "Método testado e validado em mais de 300 restaurantes",
  "Estratégias simples que funcionam mesmo para iniciantes",
  "Resultados reais desde os primeiros 7 dias",
  "Explicação prática, sem enrolação e 100% aplicável",
  "Você aprende a corrigir erros que hoje te fazem perder dinheiro",
  "Serve para delivery, salão ou operação híbrida",
];
---

<section class="w-full max-w-3xl mx-auto space-y-8 text-left px-4 py-10">

  <!-- TÍTULO -->
  <header class="space-y-2 text-center md:text-left">
    <h2 class="text-3xl font-bold leading-tight">
      Por que escolher o <span class="text-green-600">Dominando o Delivery?</span>
    </h2>
    <p class="text-gray-600 text-lg">
      Os motivos reais que fazem esse método funcionar
    </p>
  </header>

  <!-- LISTA -->
  <ul class="space-y-4">
    {items.map((item) => (
      <li class="flex items-start gap-3">
        <!-- Ícone SVG maior -->
        <span class="flex-shrink-0 flex items-center justify-center 
                     w-6 h-6 sm:w-7 sm:h-7 rounded-full bg-green-600">
          <svg class="w-3.5 h-3.5 sm:w-4 sm:h-4 text-white" fill="none" stroke="currentColor" strokeWidth="3" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" d="M5 13l4 4L19 7" />
          </svg>
        </span>

        <span class="text-lg leading-snug">
          {item}
        </span>
      </li>
    ))}
  </ul>

  <!-- CTA -->
  <div class="pt-4 text-center md:text-left">
    <a 
      href="#comprar"
      class="inline-block bg-green-600 hover:bg-green-700 transition text-white 
             px-8 py-4 rounded-xl font-semibold text-md shadow-sm"
    >
      Garantir a minha vaga agora
    </a>
  </div>

</section>

<style>
.no-padding {
  padding-top: 0 !important;
  padding-bottom: 0 !important;
}
</style>